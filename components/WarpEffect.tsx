
import React from 'react';

// Component disabled in favor of simpler transition as requested
const WarpEffect: React.FC<{ active: boolean; duration?: number }> = () => {
  return null;
};

export default WarpEffect;
